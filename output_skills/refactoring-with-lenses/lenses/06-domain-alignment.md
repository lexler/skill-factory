# Domain Alignment

Does the code speak the problem domain, or implementation language?

Look for: implementation words (nested, parse, split, loop, handler, processor) where domain words should be.

If you'd explain the code differently than it reads, there's a gap.
