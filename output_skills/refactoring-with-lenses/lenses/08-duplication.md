# Duplication

What knowledge exists in more than one place?

Look for: multiple structures encoding the same concept, parallel arrays that change together, similar transformations repeated, constants in multiple places.

The test: is the same knowledge written more than once?
