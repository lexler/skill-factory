# Patterns

What patterns exist but aren't named?

Look for: repeated structures following a template, code that "feels like" a known pattern, implicit rules that could be explicit.

Trade-off: not every pattern needs naming. Ask: does naming this make the code clearer, or just more abstract?
